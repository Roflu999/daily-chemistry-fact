 ```yaml
   name: Daily Chemistry Fact

   on:
     schedule:
       - cron: '0 5 * * *'
     workflow_dispatch:

   jobs:
     send-chemistry-fact:
       runs-on: ubuntu-latest
       steps:
         - name: Send WhatsApp Message
           run: |
             curl -X POST "https://api.callmebot.com/whatsapp.php" \
               --data-urlencode "phone=${{ secrets.971508513968 }}" \                                                                                                                   --data-urlencode "text=🧪 Daily Chemistry Fact: Gold is the only naturally yellow metal! All other yellow metals are alloys. Gold's color comes from
 relativistic effects on its electrons. ⚛️                                                                                                                                  relativistic effects on its electrons. ⚛️" \                                                                                                                               ```
               --data-urlencode "apikey=${{ secrets.8601024 }}"
 ```
